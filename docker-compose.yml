services:
  web:
    container_name: mini-ktv
    image: 192.168.0.11:5000/mini-ktv/mini-ktv:latest
    ports:
      - "8080:80"
    volumes:
      - ./db:/usr/share/nginx/html/db:ro
      - ./src/config:/usr/share/nginx/html/config:ro
    restart: unless-stopped
